<x-layout :item="$item" :preview="true"><section class="container preview-content"><span class="eyebrow">{{ __('site.preview') }} · {{ $kind }}</span><h1>{{ $item->tr('title') }}</h1><p class="lead">{{ $item->tr('excerpt') }}</p>@if($item->imageUrl())<img src="{{ $item->imageUrl() }}" width="1320" height="600" alt="{{ $item->tr('alt') }}">@endif<div class="prose preserve">{{ $item->tr('body') }}</div>@if($kind==='sectors')<ul>@foreach(explode("\n",$item->tr('services')) as $s)<li>{{ $s }}</li>@endforeach</ul>@endif @if($kind==='slides'&&$item->link)<a class="button" href="{{ url(app()->getLocale().$item->link) }}">{{ $item->tr('button') }}</a>@endif</section></x-layout>