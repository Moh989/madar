<x-layout :item="$item"><section class="page-heading container"><a class="eyebrow" href="{{ $kind==='sectors'?route('sectors'):route('listing',['kind'=>$kind]) }}">{{ __('site.'.$kind) }}</a><h1>{{ $item->tr('title') }}</h1><p>{{ $item->tr('excerpt') }}</p></section>@if($item->imageUrl())<div class="container"><img class="detail-hero" src="{{ $item->imageUrl() }}" width="1320" height="650" alt="{{ $item->tr('alt') }}"></div>@endif<section class="section container detail-grid"><div class="prose preserve">{{ $item->tr('body') }}</div><aside>@if($kind==='sectors')<h2>{{ __('site.services') }}</h2><ul class="services-list">@foreach(explode("\n",$item->tr('services')) as $service)@if($service)<li>{{ $service }}</li>@endif @endforeach</ul><a class="button button-navy" href="{{ route('quote',['sector'=>$item->id]) }}">{{ __('site.sector_inquiry') }} {{ __('site.arrow') }}</a>@else<a class="button button-navy" href="{{ route('contact') }}">{{ __('site.contact') }}</a>@endif</aside></section>@if($kind==='projects'&&$item->gallery)<section class="container section"><h2>{{ __('site.gallery') }}</h2><div class="stories">@foreach(\App\Models\Media::whereIn('id',$item->gallery)->get() as $photo)<img src="{{ route('media.show',$photo) }}" width="{{ $photo->width }}" height="{{ $photo->height }}" alt="{{ $photo->alt[app()->getLocale()]??'' }}" loading="lazy">@endforeach</div></section>@endif<x-cta/></x-layout>