<x-layout :title="__('site.'.$kind)"><section class="page-heading container"><span class="eyebrow">{{ __('site.company') }}</span><h1>{{ __('site.'.$kind) }}</h1></section><section class="container section listing-section">@if($kind==='projects')<form class="filter-form" method="get"><label for="sector-filter">{{ __('site.sector_field') }}</label><select name="sector" id="sector-filter"><option value="">{{ __('site.all') }}</option>@foreach($sectors as $sector)<option value="{{ $sector->id }}" @selected(request('sector')==$sector->id)>{{ $sector->tr('title') }}</option>@endforeach</select><button class="button button-navy">{{ __('site.filter') }}</button></form>@endif<div class="stories">@forelse($items as $item)<x-card :item="$item" :kind="$kind"/>@empty<p>{{ __('site.empty') }}</p>@endforelse</div><div class="pagination">{{ $items->links('components.pagination') }}</div></section></x-layout>