<x-layout :page="$page"><section class="page-heading container"><span class="eyebrow">{{ __('site.company') }} / {{ $page->slug==='about'?__('site.about'):__('site.privacy') }}</span><h1>{{ $page->tr('title') }}</h1></section><section class="container page-body {{ $page->slug==='about'?'about-grid':'' }}"><div><p class="lead preserve">{{ $page->tr('body') }}</p>@if($page->slug==='about')<h2 class="legal-heading" lang="ar" dir="rtl">{{ __('site.legal_name') }}</h2>@php($brand=\App\Models\Setting::valueFor('brand',[]))@if($brand['english_approved']??false)<p lang="en" dir="ltr">{{ $brand['english_legal_name'] }}</p>@endif<a class="text-link" href="{{ route('sectors') }}">{{ __('site.all_sectors') }} {{ __('site.arrow') }}</a>@if($hasProfile)<a class="button button-navy profile-home-button" href="{{ route('profile') }}"><i data-lucide="book-open"></i>{{ __('site.view_profile') }} <span>{{ __('site.arrow') }}</span></a>@endif @endif</div>@if($page->slug==='about')<img class="about-image" src="{{ asset('images/sea.webp') }}" width="800" height="850" alt="{{ __('site.sea_alt') }}">@endif</section>@if($page->slug==='about')<section class="section container"><span class="eyebrow">{{ __('site.sector_label') }}</span><h2>{{ __('site.sectors') }}</h2><div class="compact-sector-grid">@foreach($sectors as $sector)<a href="{{ route('sector',['slug'=>$sector->slug]) }}">{{ $sector->tr('title') }} <span>{{ __('site.arrow') }}</span></a>@endforeach</div></section><x-cta/>@endif</x-layout>